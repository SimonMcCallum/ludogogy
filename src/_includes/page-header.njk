{# Reusable page header banner for subpages #}
<section class="page-header">
    <div class="container">
        <h1>{{ pageTitle }}</h1>
        {% if subtitle %}<p>{{ subtitle }}</p>{% endif %}
    </div>
</section>
